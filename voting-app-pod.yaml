apiVersion: v1
kind: Pod
metadata:
  name: voting-app-pod
  labels:
    name: voting-app-pod
    app: demo-voting-pod
spec:
  containers:
    - name: votingapp-pod
      image: kodekloud/examplevotingapp_vote:v1
      ports:
        - containerPort: 80
